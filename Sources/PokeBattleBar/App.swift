import SwiftUI

struct PokeBattleBarApp: App {
    var body: some Scene {
        WindowGroup("PokeBattleBar") {
            RootView()
        }
        .defaultSize(width: 860, height: 640)
    }
}
